git merge combines code from different branches while preserving commit history, whereas git rebase linearizes history by moving or combining commits.
A remote repository is a version-controlled code repository hosted on a server, typically accessible over the internet, for sharing and collaboration.

